package Ch_039.IO.Exceptions;

//exception list for self format IO (txt)

public enum ExceptionList {

    INVALID_NAME, INVALID_LAST_NAME, INVALID_BIRTH_DATE, INVALID_ANALYZES_TYPE, INVALID_ANALYZES_DATE, DEFAULT
}
